# === Dependências ===
node_modules/
.pnp
.pnp.js
.yarn/

# === Build / saída ===
dist/
build/
out/
.next/
.nuxt/
.svelte-kit/
coverage/
*.tsbuildinfo

# === Ambiente / segredos (NUNCA commitar) ===
.env
.env.*
!.env.example
*.pem
*.key
secrets/
credentials*.json

# === Dados sensíveis / PII (LGPD — NUNCA commitar) ===
# PDFs de certidões, comprovantes, screenshots com dados pessoais
storage/
# storage local/dev do app (uploads fictícios da Fase 4 — docs/15 §3.2)
storage-local/
labs/certidoes/storage/
**/screenshots/
*.pdf
!docs/**/*-modelo.pdf
# prints de reconhecimento podem conter CPF/nome/empresa
docs/**/prints/
docs/legal/screenshots/

# === Playwright / automação (Fase 8B — docs/28) ===
playwright-report/
test-results/
.playwright/
traces/
# evidências geradas do laboratório sintético (screenshots/PNG) — não versionar;
# a pasta é mantida por tests/e2e/artifacts/.gitkeep
tests/e2e/artifacts/*
!tests/e2e/artifacts/.gitkeep

# === Fase 9 — artifacts da config segura (docs/35) — NUNCA versionar ===
# podem conter PII/evidências de tela real; a pasta é mantida pelo .gitkeep
tests/e2e/phase9-artifacts/*
!tests/e2e/phase9-artifacts/.gitkeep

# === Banco local (laboratório) ===
*.sqlite
*.sqlite3
*.db

# === Logs ===
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === Sistema operacional ===
.DS_Store
Thumbs.db
desktop.ini

# === Editores / IDE ===
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/
*.swp
*.swo

# === Claude Code (memória local do projeto fica fora do git por padrão) ===
# Descomente se NÃO quiser versionar a pasta de memória:
# .claude/
